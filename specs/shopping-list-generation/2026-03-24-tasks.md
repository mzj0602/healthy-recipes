# shopping-list-generation 任务列表

**版本**：2026-03-24
**Task Count**: 7
**执行策略**：单 Agent 执行

---

## 共享类型（优先执行）

- [x] T01 [types] 在 `src/features/health-plan/types.ts` 新增 `IngredientCategory`、`IngredientEntry`、`ShoppingItem` 类型定义

## 数据层

- [x] T02 [frontend] 创建 `src/features/health-plan/data/meal-ingredients-map.ts`，补全覆盖全部 28 条默认餐食的 `mealIngredientsMap` 静态映射表（`Record<string, IngredientEntry[]>`）

## 业务逻辑

- [x] T03 [frontend] 创建 `src/features/health-plan/utils/build-shopping-list.ts`，实现以下纯函数：
  - 遍历 `plan[].meals[]`，以 `meal.title` 查 `mealIngredientsMap`；未命中时生成占位条目（`category: '其他'`）
  - 按 `name` 去重，累加 `servings`，生成 `displayAmount`（格式：`"150g × 3 份"`）
  - 按固定顺序分组：蔬果类 → 肉蛋类 → 主食类 → 调料类 → 其他
  - 返回 `Map<IngredientCategory, ShoppingItem[]>`

## 组件层

- [x] T04 [frontend] 创建 `src/features/health-plan/components/shopping-list-modal.tsx`：
  - Props：`plan`、`onClose`、`onOpenEditPlan`
  - 调用 T03 工具函数计算购物清单
  - 内部 `useState`：`checked: Set<string>`、`copyFeedback: boolean`
  - 渲染分组列表（标题 + 复选框条目，已勾选项文字变灰 + 删除线）
  - "复制文字"按钮（检测 `navigator.clipboard` 可用性，不可用时隐藏）：点击后写入纯文本，显示"已复制！"提示 1.5s
  - 空状态：显示提示文案 + "去编辑计划"按钮（调用 `onClose` + `onOpenEditPlan`）
  - 弹窗标题包含食材总数
  - 点击蒙层或 × 按钮调用 `onClose`

- [x] T05 [frontend] 修改 `src/features/health-plan/components/health-plan-page.tsx`：
  - 新增 `isShoppingListOpen` state（`useState(false)`）
  - 在页面头部区域新增"生成购物清单"按钮，点击设为 `true`
  - 在 `EditPlanModal` 同级位置条件渲染 `<ShoppingListModal>`，传入 `plan`、`onClose`、`onOpenEditPlan`（打开编辑弹窗）

## 测试任务

- [x] T06 [test] 新增单元测试 `src/test/unit/shopping-list.test.ts`：
  - `mealIngredientsMap` 覆盖全部 28 条默认餐食，无 undefined key
  - 相同食材去重：servings 累加，displayAmount 格式正确
  - 未命中 key 时生成正确占位条目（category 为 '其他'）
  - 分类分组顺序固定（蔬果类 → 肉蛋类 → 主食类 → 调料类 → 其他），无食材的分类不出现
  - `plan` 全空时返回空 Map

- [x] T07 [test] 新增 E2E 测试 `src/test/e2e/shopping-list.spec.ts`（SPA，禁止 `page.goto()`，只能点击 UI）：
  - AC-1：点击"生成购物清单"→ 弹窗出现，标题含食材总数
  - AC-2：断言分类标题可见，食材条目带复选框
  - AC-3：点击复选框 → 文字变灰 + 删除线；再次点击 → 恢复
  - AC-4：点击"复制文字"→ 出现"已复制！"提示（mock `navigator.clipboard`）
  - AC-5：勾选后点击 × / 蒙层关闭弹窗，重新打开后勾选已清空
  - AC-6：空计划数据 → 弹窗显示空状态，"去编辑计划"按钮可用

---

**完成标准**：所有任务 [x]，`tsc --noEmit` 无报错，`pnpm test` 全部通过
