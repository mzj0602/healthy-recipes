# header-search 任务列表

**版本**：2026-03-23
**Task Count**: 4
**执行策略**：单 Agent 执行

---

## 前端任务

- [x] T01 [frontend] 在 `src/features/meal-planner/components/site-chrome.tsx` 中，SiteChrome 函数之前内联定义 `HeaderSearch` 组件：
  - 接收 `activePage: PlannerPageId` 和 `onNavigate: (page: PlannerPageId) => void` props
  - 本地 `isExpanded` / `localValue` state；`useRef` 用于 input 自动聚焦
  - `useEffect` 监听 `isExpanded` 变化，展开时 `inputRef.current?.focus()`
  - `handleSubmit`：trim 后非空才写 `searchAtom`，仅在非 recipes 页调用 `onNavigate('recipes')`
  - `handleKeyDown`：Enter → `handleSubmit`，Escape → 收起并清空
  - `onBlur + setTimeout(150ms)` 实现点击外部收起
  - CSS transition 控制宽度（展开 `w-[220px] opacity-100`，收起 `w-0 opacity-0`）
  - 在 SiteChrome 的 header 区域（个人中心图标左侧）渲染 `<HeaderSearch activePage={activePage} onNavigate={onNavigate} />`

- [x] T02 [frontend] 修改 `src/features/meal-planner/components/recipe-explore-page.tsx`：
  - 将 `const [query, setQuery] = useState('');` 替换为 `const [query, setQuery] = useAtom(searchAtom);`
  - 补充必要 import（`useAtom` from `jotai`，`searchAtom` from `@/features/recipes/store/recipeAtoms`）
  - 删除不再需要的 `useState` import（如已无其他用途）

## 测试任务

- [x] T03 [test] 新增单元测试 `src/test/unit/header-search.test.tsx`，覆盖：
  - 点击搜索图标后 input 出现（`isExpanded` → true）
  - 输入空字符串按 Enter：`searchAtom` 不被写入，`onNavigate` 不被调用
  - 输入关键词按 Enter：`searchAtom` = 关键词，`onNavigate('recipes')` 被调用
  - 已在 recipes 页搜索：`onNavigate` 不被重复调用
  - 按 Esc 键：input 收起，`localValue` 清空

- [x] T04 [test] 新增 E2E 测试 `src/test/e2e/header-search.spec.ts`，覆盖（全程点击 UI，禁止 `page.goto()`）：
  - 流程 1：从首页点击搜索图标 → 输入"三文鱼" → Enter → 验证探索页搜索栏显示"三文鱼"且菜谱列表已过滤
  - 流程 2：导航至菜谱探索页 → Header 搜索"高蛋白" → 验证菜谱列表刷新，无页面切换闪烁
  - 流程 3：展开搜索框不输入直接 Enter → 验证页面仍为首页
  - 流程 4：展开搜索框后点击外部区域 → 验证搜索框收起

---
**完成标准**：所有任务 [x]，`tsc --noEmit` 无报错，`pnpm test` 全部通过
