# user-login 任务列表

**版本**：2026-03-28
**Task Count**: 6
**执行策略**：单 Agent

---

## 前端任务
- [x] T01 [frontend] 在 `src/features/meal-planner/components/login-page.tsx` 创建轻量登录页，包含用户名/密码输入、错误提示与登录按钮
- [x] T02 [frontend] 在 `src/features/meal-planner/components/healthy-meal-planner-app.tsx` 增加登录态判断、登录成功进入主应用、退出后回到登录页
- [x] T03 [frontend] 在 `src/features/meal-planner/components/site-chrome.tsx` 增加当前用户名展示和退出按钮，并保持现有导航/搜索不受影响
- [x] T04 [frontend] 使用 `sessionStorage` 实现当前浏览器会话内的登录态恢复，并保证退出时清理

## 测试任务
- [x] T05 [test] 新增单元测试 `src/test/unit/login-page.test.tsx`，覆盖空值、错误凭证、正确登录三类核心场景
- [x] T06 [test] 新增 E2E 测试 `src/test/e2e/login.spec.ts`，覆盖登录、刷新保持、退出三个关键流程，并更新必要的现有测试适配新 props

---
**完成标准**：所有任务 [x]，tsc --noEmit 无报错，pnpm test 全部通过
