# login-stitch-sync 任务列表

**版本**：2026-04-02
**Task Count**: 5
**执行策略**：单 Agent（任务 ≤ 8，纯前端）

---

## 前端任务

- [x] T01 [frontend] 在 `index.html` `<head>` 中添加 Google Fonts Epilogue `<link>` 预连接和样式表标签（wght@700;800，display=swap）
- [x] T02 [frontend] 重构 `src/features/meal-planner/components/login-page.tsx`：将整体布局改为双列（`flex min-h-screen`），添加左侧 Hero 区（`hidden lg:flex w-[55%]`，食物大图 + 渐变遮罩 + 品牌 Tagline）
- [x] T03 [frontend] 在右侧表单区顶部添加导航条（FreshPlate 品牌文字 + 「注册」视觉展示按钮），右列容器使用 Harvest 暖奶油背景（`#fdf9f3`）
- [x] T04 [frontend] 更新表单内各元素样式：输入框改为无边框暖灰底色（`bg-[#f7f3ed] border-none`），登录按钮改为渐变橙色（`linear-gradient(135deg, #964900, #f5821f)`），添加「记住我」复选框与「忘记密码」链接（仅视觉），演示账号提示改用 `bg-[#f7f3ed]` 背景
- [x] T05 [frontend] 将主标题「账号登录」改用 Epilogue 字体（`style={{ fontFamily: 'Epilogue, serif' }}`），确保所有现有业务逻辑（handleSubmit、demoAccount、onLogin 回调、E2E 选择器兼容项）完整保留，运行 `tsc --noEmit` 验证

---

**完成标准**：所有任务 [x]，tsc --noEmit 无报错，pnpm test 全部通过
