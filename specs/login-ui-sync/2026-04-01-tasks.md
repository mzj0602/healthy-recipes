# login-ui-sync 任务列表

**版本**：2026-04-01
**Task Count**: 13
**执行策略**：单 Agent 串行执行

---

## 前端任务

- [x] T01 [frontend] 在 `src/features/meal-planner/components/login-page.tsx` 顶部新增 `Input`、`Button`、`cn` 的 import（来自 `@/components/ui/input`、`@/components/ui/button`、`@/lib/utils`）
- [x] T02 [frontend] 将外层容器 `bg-[#f9f8f6]` 替换为 `bg-background`（文件：`login-page.tsx`）
- [x] T03 [frontend] 将卡片容器 `border-[#f2dfcf]` 替换为 `border-border-input`，`shadow-[0_20px_60px_rgba(63,38,23,0.08)]` 替换为 `shadow-lg`，背景色保留 `bg-card`（文件：`login-page.tsx`）
- [x] T04 [frontend] 将 Logo 区背景 `bg-[#ec7f13]` 替换为 `bg-primary`，文字色替换为 `text-primary-foreground`（文件：`login-page.tsx`）
- [x] T05 [frontend] 将品牌名 `text-[#ec7f13]` 替换为 `text-primary`，标题 `text-[#3f2617]` 替换为 `text-foreground`，副标题 `text-[#8b6b55]` 替换为 `text-muted-foreground`（文件：`login-page.tsx`）
- [x] T06 [frontend] 将演示账号提示区背景 `bg-[#fff4e8]` 替换为 `bg-accent-background`，文字 `text-[#9a5b1a]` 替换为 `text-accent-foreground`（文件：`login-page.tsx`）
- [x] T07 [frontend] 将两处 Label 文字色 `text-[#5d4230]` 替换为 `text-foreground`，两处图标色 `text-[#aa8367]` 替换为 `text-muted-foreground`（文件：`login-page.tsx`）
- [x] T08 [frontend] 将用户名原生 `<input>` 替换为 shadcn `<Input>`，保留 `id`、`type`、`autoComplete`、`value`、`onChange`、`placeholder`，className 改为 `"py-3 pl-10 pr-4"`（文件：`login-page.tsx`）
- [x] T09 [frontend] 将密码原生 `<input>` 替换为 shadcn `<Input>`，保留同上属性，className 改为 `"py-3 pl-10 pr-4"`（文件：`login-page.tsx`）
- [x] T10 [frontend] 将登录原生 `<button>` 替换为 shadcn `<Button variant="default" size="lg">`，保留 `type`、`disabled`、子文本，className 改为 `"w-full font-semibold"`（文件：`login-page.tsx`）

## 测试任务

- [x] T11 [test] 新建单元测试文件 `src/features/meal-planner/components/__tests__/login-page.test.tsx`，覆盖以下用例：渲染 shadcn Input（确认两个 `role="textbox"`）、渲染 shadcn Button（确认文字为"登录"）、空字段提交（出现"请输入用户名和密码"）、错误凭证提交（出现"用户名或密码错误"）、正确凭证提交（`onLogin` 被调用且参数含 `{ username: 'demo' }`）
- [x] T12 [test] 在 T11 的测试文件中追加"无硬编码 hex"测试用例：渲染后断言所有元素的 className 不包含 `#` 开头的颜色值
- [x] T13 [test] 新建 E2E 测试文件 `e2e/login-ui-sync.spec.ts`，覆盖以下用例：登录页正常渲染（"账号登录"标题与演示账号提示可见）、空字段校验（点击登录后出现错误提示）、错误凭证（填入 wrong/wrong 后出现"用户名或密码错误"）、正确登录（填入 demo/123456 后主应用页面可见）；测试从 `page.goto('/')` 开始，不使用 `page.goto('/login')`

---
**完成标准**：所有任务 [x]，`tsc --noEmit` 无报错，`pnpm test` 全部通过
