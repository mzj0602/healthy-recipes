# diet-plan-edit 任务列表

**版本**：2026-03-24
**Task Count**: 7
**执行策略**：单 Agent 执行

---

## 前端任务

- [x] T01 [frontend] 在 `src/features/health-plan/components/health-plan-page.tsx` 新增 `loadFromStorage` / `handleSave` 工具函数及 `plan`、`goals`、`isEditOpen` 状态；初始化从 `localStorage('health-plan-data')` 读取，回退默认数据；挂载 `<EditPlanModal>` 条件渲染
- [x] T02 [frontend] 实现 `src/features/health-plan/components/edit-plan-modal.tsx`：弹窗框架（Dialog/蒙层）、Props 接口（`plan / goals / onSave / onClose`）、`draftPlan / draftGoals / activeDayId / errors` 状态初始化（深拷贝）
- [x] T03 [frontend] 在 `edit-plan-modal.tsx` 实现日期 Tab 切换：渲染七天 Tab 栏，点击更新 `activeDayId`，展示对应 `draftPlan` 餐食列表
- [x] T04 [frontend] 在 `edit-plan-modal.tsx` 实现餐食行编辑：`title` / `calories` `<input>` 双向绑定更新 `draftPlan`；添加餐食（type=其他）；删除餐食（仅剩1条时 disabled）
- [x] T05 [frontend] 在 `edit-plan-modal.tsx` 实现营养目标编辑：`percent` `<input>` 实时截断（`Math.round(Math.min(100, Math.max(0, ...)))`），更新 `draftGoals`
- [x] T06 [frontend] 在 `edit-plan-modal.tsx` 实现保存校验：点击保存时遍历全部七天所有餐食，title 为空或 calories 非有效非负整数时写入 `errors` 并阻止保存；校验通过后调用 `onSave(draftPlan, draftGoals)`；取消/×/蒙层点击调用 `onClose`

## 测试任务

- [x] T07 [test] 补全单元测试 `src/test/unit/edit-plan-modal.test.tsx`：深拷贝隔离、title/calories 修改、校验报错（空 title、非整数 calories）、单条时删除 disabled、percent 截断、取消不调用 onSave（共 7 项用例）
- [x] T08 [test] 补全 E2E 测试 `src/test/e2e/diet-plan-edit.spec.ts`：点击导航至健康计划页（SPA 状态路由，禁用 goto）→ 打开弹窗 → Tab 切换 → 编辑保存持久化 → 取消还原 → 添加/删除餐食 → 校验报错（覆盖 AC-1 ~ AC-10）

---
**完成标准**：所有任务 [x]，`tsc --noEmit` 无报错，`pnpm test` 全部通过
